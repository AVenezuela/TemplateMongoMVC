@model IEnumerable<TemplateMongo.Model.Address>
<ul class="list-group" data-prefix="@Html.ViewData.TemplateInfo.HtmlFieldPrefix" id="@ViewBag.containerListID">
    @foreach (var item in Model)
    {
        @Html.EditorFor(modelItem => item, null, "");
    }
</ul>
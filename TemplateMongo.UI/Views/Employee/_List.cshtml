@model TemplateMongo.ViewModel.EmployeeViewModel
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeBag.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeBag.BirthDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody id="lstEmployees">
        @foreach (var item in Model.Employees)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BirthDate)
                </td>
                <td class="text-right">
                    @Html.ActionLink(" ", "Action", new { id = item.MongoID }, new { @class = "glyphicon glyphicon-edit", @title = "Edit", @id = "iconEdit" })
                    @Html.ActionLink(" ", "Action", new { id = item.MongoID }, new { @class = "glyphicon glyphicon-remove text-danger", @title = "Delete", @id = "iconDelete" })
                </td>
            </tr>
        }
    </tbody>
</table>
<div class="form-group text-center">
    @Html.PagerEditor()
</div>

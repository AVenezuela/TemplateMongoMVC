<div class="x_panel">
    <div class="x_title">
        <h2>{{PanelInfo.baseTitle}} <small>{{PanelInfo.subTitle}}</small></h2>
        <ul class="nav navbar-right panel_toolbox">
            <li>
                <a id="lblGridEmployee" class="collapse-link"><i class="fa fa-chevron-up"></i></a>
            </li>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
                <ul class="dropdown-menu" role="menu">
                    <li ng-repeat="item in PanelInfo.actionMenuItems">
                        <a ng-click="callFunction(item.Action)"><i class="fa {{item.Icon}}"></i>{{item.Name}}</a>
                    </li>
                </ul>
            </li>           
        </ul>
        <div class="clearfix"></div>
    </div>
    <div class="x_content">
        <div class="slide-animate" ng-include="PanelInfo.actionTemplate"></div>
        <table datatable="" dt-options="dtOptions" dt-columns="dtColumns" dt-instance="PanelInfo.dtInsuranceCo" class="table table-striped table-bordered dt-responsive nowrap" width="100%" cellspacing="0"></table>        
    </div>
</div>

